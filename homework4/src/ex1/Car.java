package ex1;

public class Car extends Machine {
    private final boolean HAVE_CRUISE_CONTROL = true;

    public Car(boolean haveDirt, double length, double height, double width) {
        super(haveDirt, length, height, width);
    }
}
